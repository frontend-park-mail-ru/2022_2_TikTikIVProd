{{#each this.config.groups as |group|}}
<div class="settings-group">
        {{#each group.inputs as |inpt|}}
        <div class="settings-input-with-title">
            <div class="settings-input-with-title__title">
                {{inpt.title}}
            </div>
            <input type={{inpt.type}} id={{inpt.id}} value="{{lookup ../../this.data inpt.id}}" class="settings-input-with-title__input" />
            <div id="{{inpt.id}}-msg" class="settings-input-with-title__error-msg"></div>
        </div>
        {{/each}}
    <button class="settings-group__button settings-group__button--small" data-action="submit">
        ok
    </button>
</div>
{{/each}}